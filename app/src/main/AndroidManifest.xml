<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeoneboxmess.lalit_.jonebox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.FlashActivity">

        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.Registration" />
        <activity
            android:name=".activity.MainDashBoard"
            android:label="@string/title_activity_main_dash_board"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".activity.ViewAccountDetails" />
        <activity android:name=".activity.Bill" />
        <activity android:name=".activity.Transaction" />
        <activity android:name=".CancelTransaction" />
        <activity android:name=".activity.CancelTifin">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>